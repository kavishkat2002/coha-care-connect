/**
 * Authentication & Role Types
 * Domain types for user roles across the Coha Care Connect platform.
 */

/** All supported user roles within the platform. */
export type Role = "patient" | "doctor" | "hospital" | "admin" | "elab" | "pharmacy";
